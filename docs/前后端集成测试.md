# 前后端集成测试文档

## 实现状态

### 后端API实现 ✅
- [x] 用户登录接口 `/api/login` (POST)
- [x] 用户管理接口 `/api/users` (GET, POST, PUT, DELETE)
- [x] 数据库用户表和相关操作
- [x] 错误处理和响应格式统一

### 前端集成实现 ✅
- [x] 登录页面集成后端API
- [x] 用户管理页面集成后端API
- [x] API服务封装 (`userService.js`)
- [x] 统一API配置 (`config.js`)
- [x] 错误处理和用户反馈

## 测试步骤

### 1. 后端服务测试

#### 启动后端服务
```bash
cd "d:/Cpp Project/FaceMeetingSystem/backend/build"
./FaceMeetingServer.exe
```

#### 测试用户登录API
```bash
# 测试管理员登录
curl -X POST http://localhost:18080/api/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'

# 测试普通用户登录
curl -X POST http://localhost:18080/api/login \
  -H "Content-Type: application/json" \
  -d '{"username":"user1","password":"password123"}'
```

#### 测试用户管理API
```bash
# 获取用户列表
curl -X GET http://localhost:18080/api/users

# 创建新用户
curl -X POST http://localhost:18080/api/users \
  -H "Content-Type: application/json" \
  -d '{"username":"testuser","password":"testpass"}'

# 更新用户
curl -X PUT http://localhost:18080/api/users/3 \
  -H "Content-Type: application/json" \
  -d '{"username":"testuser_updated","password":"newpass"}'

# 删除用户
curl -X DELETE http://localhost:18080/api/users/3
```

### 2. 前端服务测试

#### 启动前端服务
```bash
cd "d:/Cpp Project/FaceMeetingSystem/frontend"
npm run dev
```

#### 访问前端应用
- 打开浏览器访问: http://localhost:3000/
- 测试登录功能
- 测试用户管理功能

### 3. 集成测试场景

#### 场景1: 管理员登录和用户管理
1. 使用管理员账号登录 (admin/admin123)
2. 验证跳转到用户管理页面
3. 查看用户列表
4. 添加新用户
5. 编辑用户信息
6. 删除用户

#### 场景2: 普通用户登录
1. 使用普通用户账号登录 (user1/password123)
2. 验证跳转到会议室查询页面
3. 验证无法访问管理员功能

#### 场景3: 错误处理测试
1. 测试错误的用户名/密码
2. 测试网络连接失败情况
3. 测试重复用户名创建
4. 测试删除不存在的用户

## 当前数据库用户

| ID | 用户名 | 密码 | 角色 |
|----|--------|------|------|
| 1  | admin  | admin123 | admin |
| 2  | user1  | password123 | user |

## API响应格式

### 登录成功响应
```json
{
  "id": 1,
  "username": "admin",
  "role": "admin",
  "face_token": ""
}
```

### 用户列表响应
```json
[
  {
    "id": 1,
    "username": "admin",
    "face_token": ""
  },
  {
    "id": 2,
    "username": "user1",
    "face_token": ""
  }
]
```

### 错误响应
```json
{
  "error": "错误描述信息"
}
```

## 下一步开发计划

1. **会议室管理功能集成**
   - 实现会议室的增删改查
   - 集成前端会议室管理页面

2. **会议预定功能集成**
   - 实现会议的创建和查询
   - 集成前端会议预定页面

3. **签到功能集成**
   - 实现签到记录的创建和查询
   - 集成前端会议室终端页面

4. **人脸识别功能**
   - 集成人脸识别库
   - 实现人脸注册和识别

5. **权限控制优化**
   - 实现基于JWT的身份验证
   - 完善前端路由守卫

## 注意事项

1. 确保后端服务在 `http://localhost:18080` 运行
2. 确保前端服务在 `http://localhost:3000` 运行
3. 检查浏览器控制台是否有CORS错误
4. 确保数据库文件 `meeting_system.db` 存在且可写
5. 测试时注意观察后端日志输出